from __future__ import annotations


def run_watcher() -> dict[str, str]:
    return {
        "status": "disabled",
        "reason": "Configure Gmail OAuth credentials and approval policy before enabling the watcher.",
    }

